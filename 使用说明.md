# MyDiary Flutter 版本使用说明

## 项目简介

这是 MyDiary 应用的 Flutter 重写版本，支持多平台运行：
- ✅ Android
- ✅ iOS  
- ✅ Windows
- ✅ Linux
- ✅ macOS

## 功能特性

### 核心功能
- 📝 **日记管理**: 创建、编辑、删除日记条目
- 📅 **日历视图**: 按日期浏览和管理日记
- 🏷️ **主题分类**: 为日记添加主题标签
- 📱 **联系人管理**: 管理相关联系人信息
- 🔒 **密码保护**: 应用启动密码保护
- 🌙 **主题切换**: 支持浅色/深色主题

### 高级功能
- 📷 **照片支持**: 为日记添加图片
- 📍 **位置记录**: 自动获取和记录位置信息
- 🌤️ **天气信息**: 记录当时的天气状况
- 😊 **心情记录**: 选择和记录心情状态
- 🔍 **搜索功能**: 快速搜索日记内容
- 💾 **备份导出**: 数据备份和导出功能
- 🌐 **多语言**: 支持中文和英文界面

## 安装和运行

### 前置要求

#### 所有平台
- Flutter SDK 3.5.4 或更高版本
- Dart SDK 3.0.0 或更高版本

#### Windows 特定要求
- Windows 10 或更高版本
- Visual Studio 2022 或 Visual Studio Build Tools 2022
- 必需的 Visual Studio 组件：
  - MSVC v143 - VS 2022 C++ x64/x86 build tools
  - Windows 10/11 SDK
  - CMake tools for Visual Studio

#### macOS 特定要求
- macOS 10.14 或更高版本
- Xcode 12.0 或更高版本
- CocoaPods

#### Linux 特定要求
- Ubuntu 18.04 或更高版本（或等效的 Linux 发行版）
- 必需的系统库：
  ```bash
  sudo apt-get install clang cmake ninja-build pkg-config libgtk-3-dev
  ```

### 获取代码

```bash
git clone https://github.com/namjagbrawa1/MyDiary_1.git
cd MyDiary_1
git checkout flutter-rewrite
cd mydiary_flutter
```

### 安装依赖

```bash
flutter pub get
```

### 运行应用

#### Android
```bash
flutter run -d android
```

#### iOS (需要 macOS)
```bash
flutter run -d ios
```

#### Windows
```bash
flutter run -d windows
```

#### Linux
```bash
flutter run -d linux
```

#### macOS
```bash
flutter run -d macos
```

### 构建发布版本

#### Android APK
```bash
flutter build apk --release
```

#### iOS (需要 macOS 和 Xcode)
```bash
flutter build ios --release
```

#### Windows 可执行文件
```bash
flutter build windows --release
```

#### Linux 可执行文件
```bash
flutter build linux --release
```

#### macOS 应用
```bash
flutter build macos --release
```

## 应用使用指南

### 首次启动

1. **设置密码**: 首次启动时，您可以选择设置应用密码
2. **权限授权**: 根据需要授权位置、相机、存储等权限
3. **语言选择**: 在设置中选择您偏好的语言

### 基本操作

#### 创建日记
1. 点击主界面的 "+" 按钮
2. 填写日记标题和内容
3. 选择日期和时间
4. 添加主题标签（可选）
5. 添加照片（可选）
6. 记录位置和天气（可选）
7. 选择心情（可选）
8. 点击保存

#### 浏览日记
- **列表视图**: 在主界面查看所有日记
- **日历视图**: 点击日历图标，按日期浏览
- **搜索**: 使用搜索功能快速找到特定日记

#### 编辑日记
1. 在日记列表中点击要编辑的日记
2. 点击编辑按钮
3. 修改内容后保存

#### 删除日记
1. 长按日记条目
2. 选择删除选项
3. 确认删除

### 高级功能

#### 主题管理
1. 进入设置界面
2. 选择"主题管理"
3. 创建、编辑或删除主题标签

#### 联系人管理
1. 进入设置界面
2. 选择"联系人管理"
3. 添加、编辑或删除联系人信息

#### 数据备份
1. 进入设置界面
2. 选择"备份与导出"
3. 选择备份格式和位置
4. 执行备份操作

#### 密码设置
1. 进入设置界面
2. 选择"安全设置"
3. 设置或修改应用密码

## 故障排除

### 常见问题

#### 1. 数据库初始化错误
**错误信息**: "databaseFactory not initialized"
**解决方案**: 这个问题已在最新版本中修复。请确保使用最新的代码。

#### 2. Windows 构建错误
**错误信息**: "unable to find directory entry in pubspec.yaml"
**解决方案**: 
```bash
flutter clean
flutter pub get
flutter run -d windows
```

#### 3. 权限问题
**问题**: 无法访问相机、位置等功能
**解决方案**: 
- Android: 在应用设置中手动授权权限
- iOS: 在系统设置中授权应用权限
- 桌面平台: 确保系统防火墙允许应用访问

#### 4. 构建失败
**解决方案**:
1. 检查 Flutter 环境: `flutter doctor -v`
2. 清理项目: `flutter clean`
3. 重新获取依赖: `flutter pub get`
4. 检查平台特定要求是否满足

### 性能优化建议

1. **发布构建**: 始终使用 `--release` 标志构建生产版本
2. **图片优化**: 压缩添加到日记中的图片
3. **定期清理**: 定期删除不需要的日记和图片
4. **备份数据**: 定期备份重要数据

## 开发信息

### 技术栈
- **框架**: Flutter 3.24.5
- **语言**: Dart 3.5.4
- **数据库**: SQLite (sqflite)
- **状态管理**: Provider
- **本地化**: flutter_localizations

### 主要依赖
- `sqflite`: 本地数据库
- `sqflite_common_ffi`: 桌面平台数据库支持
- `provider`: 状态管理
- `image_picker`: 图片选择
- `geolocator`: 位置服务
- `shared_preferences`: 本地存储
- `table_calendar`: 日历组件

### 项目结构
```
lib/
├── main.dart                 # 应用入口
├── models/                   # 数据模型
├── providers/                # 状态管理
├── screens/                  # 界面页面
├── services/                 # 业务服务
├── utils/                    # 工具类
└── widgets/                  # 自定义组件
```

## 贡献和反馈

如果您遇到问题或有改进建议，请：
1. 查看 [GitHub Issues](https://github.com/namjagbrawa1/MyDiary_1/issues)
2. 创建新的 Issue 描述问题
3. 提交 Pull Request 贡献代码

## 许可证

本项目采用 MIT 许可证。详情请查看 LICENSE 文件。

---

**注意**: 这是从原 Android 版本重写的 Flutter 版本，保持了所有原有功能，并增加了多平台支持。如果您在使用过程中遇到任何问题，请参考故障排除部分或联系开发者。